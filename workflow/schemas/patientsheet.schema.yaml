$schema: "https://json-schema.org/draft/2020-12/schema"

description: an entry in the tissue sheet

properties:
  patient_id:
    description: patient identifier
  condition:
    type: string
    description: condition of the patient

required:
  - patient_id
